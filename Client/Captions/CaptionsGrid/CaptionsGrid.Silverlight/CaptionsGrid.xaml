<!--
Project: ClipFlair (http://ClipFlair.codeplex.com)
Filename: CaptionsGrid.xaml
Version: 20130124
-->
  
  <UserControl x:Class="ClipFlair.CaptionsGrid.CaptionsGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" d:DesignHeight="167" d:DesignWidth="856"
    xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
    xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit" 
    xmlns:captions="clr-namespace:ClipFlair.CaptionsGrid"
   >
    
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/CaptionsGrid;component/Themes/Generic.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>

  <Grid x:Name="LayoutRoot" 
        Background="White"
        >
    <Grid.RowDefinitions>
      <RowDefinition Height="29" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    
    <toolkit:WrapPanel Name="Toolbar"
                Grid.Row="0"
                Orientation="Horizontal"
                HorizontalAlignment="Stretch"
                >
      <toolkit:WrapPanel.Resources>
        <Style TargetType="Button">
          <Setter Property="Margin" Value="3,4,3,4" />
        </Style>
      </toolkit:WrapPanel.Resources>
      
      <Button Name="btnAdd" Click="btnAdd_Click">
        <ToolTipService.ToolTip>
          <ToolTip Content="{Binding tooltip_caption_add, Source={StaticResource localizedStrings}}"/>
        </ToolTipService.ToolTip>
        <Image Source="Images/caption_add.png"/>
      </Button>
      
      <Button Name="btnRemove" Click="btnRemove_Click">
        <ToolTipService.ToolTip>
          <ToolTip Content="{Binding tooltip_caption_remove, Source={StaticResource localizedStrings}}"/>
        </ToolTipService.ToolTip>
        <Image Source="Images/caption_remove.png"/>
      </Button>

      <TextBlock Text="|" VerticalAlignment="Center" Foreground="DarkGray" />

      <Button Name="btnStart" Click="btnStart_Click">
        <ToolTipService.ToolTip>
          <ToolTip Content="{Binding tooltip_caption_start, Source={StaticResource localizedStrings}}"/>
        </ToolTipService.ToolTip>
        <Image Source="Images/caption_start.png"/>
      </Button>

      <Button Name="btnEnd" Click="btnEnd_Click">
        <ToolTipService.ToolTip>
          <ToolTip Content="{Binding tooltip_caption_end, Source={StaticResource localizedStrings}}"/>
        </ToolTipService.ToolTip>
        <Image Source="Images/caption_end.png"/>
      </Button>

      <TextBlock Text="|" VerticalAlignment="Center" Foreground="DarkGray" />

      <Button Name="btnImport" Content="Import" Click="btnImport_Click">
        <ToolTipService.ToolTip>
          <ToolTip Content="{Binding tooltip_captions_import, Source={StaticResource localizedStrings}}"/>
        </ToolTipService.ToolTip>
      </Button>
      
      <Button Name="btnExport" Content="Export" Click="btnExport_Click">
        <ToolTipService.ToolTip>
          <ToolTip Content="{Binding tooltip_captions_export, Source={StaticResource localizedStrings}}"/>
        </ToolTipService.ToolTip>
      </Button>
        
    </toolkit:WrapPanel>
    
    <sdk:DataGrid Name="gridCaptions"
                  Grid.Row="1" 
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                  AutoGenerateColumns="False"
                  RowBackground="White"
                  AlternatingRowBackground="Beige"
                  ItemsSource="{Binding Path=Captions.Children, RelativeSource={RelativeSource AncestorType=captions:CaptionsGrid}}">
      <sdk:DataGrid.Columns>
        <sdk:DataGridTemplateColumn Header="Start" CellTemplate="{StaticResource StartTimeCellTemplate}" CellEditingTemplate="{StaticResource StartTimeCellEditTemplate}" />
        <sdk:DataGridTemplateColumn Header="End" CellTemplate="{StaticResource EndTimeCellTemplate}" CellEditingTemplate="{StaticResource EndTimeCellEditTemplate}" />
        <sdk:DataGridTemplateColumn Header="Duration" CellTemplate="{StaticResource DurationCellTemplate}" CellEditingTemplate="{StaticResource DurationCellEditTemplate}" />
        <sdk:DataGridTemplateColumn Header="Role" CellTemplate="{StaticResource RoleCellTemplate}" CellEditingTemplate="{StaticResource RoleCellEditTemplate}" />
        <sdk:DataGridTemplateColumn Header="Caption" Width="*" CellTemplate="{StaticResource CaptionCellTemplate}" CellEditingTemplate="{StaticResource CaptionCellEditTemplate}" />
        <sdk:DataGridTemplateColumn Header="WPM" Width="50" CellTemplate="{StaticResource WPMCellTemplate}" IsReadOnly="True" />
        <sdk:DataGridTemplateColumn Header="Audio" Width="100" CellTemplate="{StaticResource AudioCellTemplate}" IsReadOnly="True" />
        <sdk:DataGridTemplateColumn Header="Comments" Width="100" CellTemplate="{StaticResource CommentsCellTemplate}" CellEditingTemplate="{StaticResource CommentsCellEditTemplate}" />
      </sdk:DataGrid.Columns>
    </sdk:DataGrid>
  </Grid>
  
</UserControl>
